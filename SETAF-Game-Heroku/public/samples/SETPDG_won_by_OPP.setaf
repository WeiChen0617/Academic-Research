arg(a).
arg(b1).
arg(b2).
arg(c).
arg(d).
arg(e).

att(r1,b1).
mem(r1,a).

att(r2,b2).
mem(r2,a).

att(r3,c).
mem(r3,b1).
mem(r3,b2).

att(r4,d).
mem(r4,c).

att(r5,c).
mem(r5,e).

att(r6,e).
mem(r6,d).
