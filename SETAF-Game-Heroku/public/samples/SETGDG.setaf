arg(a).
arg(b1).
arg(b2).
arg(c).
arg(d).
arg(e).
arg(f).

att(r1,b1).
mem(r1,a).

att(r2,b2).
mem(r2,a).

att(r3,f).
mem(r3,a).

att(r4,f).
mem(r4,b1).
mem(r4,b2).

att(r5,c).
mem(r5,b1).
mem(r5,b2).

att(r6,d).
mem(r6,c).

att(r7,e).
mem(r7,c).

att(r8,d).
mem(r8,e).

